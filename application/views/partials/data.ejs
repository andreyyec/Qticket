<!-- Data -->
<% if(locals.token !== undefined){ %>
    <script id="data" type="text/javascript">
        <% for(var i=0; i < token.length; i++) {%>
            window.sessionStorage.setItem(token[i].key, token[i].value);
        <% } %>
        console.log(token);
    </script>
<% } %>
<!-- ./Data -->